# jds-branded-ordering
jds-branded-ordering

Persistent synthetic staging-review deployment is documented in
`docs/STAGING_REVIEW_DEPLOYMENT.md`. The staging package is intentionally
separate from localhost review and must never inherit production credentials or
data.
